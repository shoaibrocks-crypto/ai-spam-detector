<resources>
    <!-- Use 100% native Android framework theme (Zero external library dependencies) -->
    <style name="Theme.AiSpamDetector" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:statusBarColor">@color/bg_app</item>
        <item name="android:navigationBarColor">@color/bg_app</item>
        <item name="android:windowBackground">@color/bg_app</item>
    </style>
</resources>
